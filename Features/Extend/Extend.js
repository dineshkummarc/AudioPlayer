/*
 * Helper Function
 * Given an object, check it against an internal object 
 * Based on the jQuery Extend function
 * if a key exists in both objects, copy over to the original key
 * Arguments = New object, shallow(one pass only), or deep(Recursively over all objects) 
 * (What level to go to in int?) Possibly later 
 *  author: Max McNally maxmcnally@gmail.com
 */


function shallow(newObject, depth){
	if(depth === 'Shallow' || depth ==='shallow'){
		
		
		
	}
	else if (depth == 'deep' || depth === 'Deep'){
		deep(newObject);
		
	}
	
	
}

function deep(){
	
	
	
}